export namespace defaultOptions {
    const backgroundColor: string;
    const textHeaderColor: string;
    const textDefaultColor: string;
    const selectedTextColor: string;
    const mainColor: string;
    const textSecondaryColor: string;
    const borderColor: string;
    const defaultFont: string;
    const headerFont: string;
    const textFontSize: number;
    const textHeaderFontSize: number;
    const headerAnimationDistance: number;
    const daysAnimationDistance: number;
    const daysStyle: {};
    const is24Hour: boolean;
}
export function TimeDatePicker(props: any): JSX.Element;
export function useCalendar(): any;
